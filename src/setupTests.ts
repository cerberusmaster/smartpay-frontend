import '@testing-library/jest-dom';  // adds custom jest matchers like toBeInTheDocument()

// Optionally, you can add other global mocks or setup here

// Example: mock window.matchMedia if you use Material UI or similar libs
if (!window.matchMedia) {
    
}
